#include <stdio.h>
int main(){

    int a;
    char x;
    char b;
    scanf("%d %c %c", &a,%x,&b);
    if(a='a' && a='e' && a='i' && a='o' && a='u'){
        printf("Vowel");
    }
    
    return 0;
}