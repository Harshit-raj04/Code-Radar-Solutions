#include <stdio.h>
int main(){

    int a;
    char x;
    char b;
    scanf("%d %c %c", &a,&x,&b);
    if(x='a' && x='e' && x='i' && x='o' && x='u'){
        printf("Vowel");
    }
    
    return 0;
}